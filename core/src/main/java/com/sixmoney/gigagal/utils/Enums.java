package com.sixmoney.gigagal.utils;

public class Enums {
    public enum JumpState {GROUNDED, JUMPING, FALLING, RECOILING}
    public enum WalkState {STANDING, WALKING}
    public enum Direction {LEFT, RIGHT}
    public enum GunType {PISTOL, CANNON, RAPID, NUKE_CANNON}
}
